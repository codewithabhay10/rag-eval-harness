"""rag_eval — a measurable RAG system: ingestion, retrieval, generation, eval.

The point of this package is not the RAG demo but the eval harness + ablation that
prove which design choices improve quality. Run modules from the project root, e.g.
`python -m rag_eval.ingestion.run`, so top-level `config` is importable.
"""
