# secrets
.env

# python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# data & model artifacts (regenerated by the pipeline) ...
/data/*
# ... except the curated test set, which is version-controlled for reproducibility
!/data/testset.json

# keep the ablation summary; ignore the noisy per-run eval dumps
results/eval_*
install.log
*.log

# HF / model caches if pointed locally
.cache/

# OS / editor
.DS_Store
.idea/
.vscode/
