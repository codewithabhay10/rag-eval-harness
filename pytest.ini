[pytest]
# We use test FUNCTIONS only; restrict class collection so the TestItem dataclass
# (in eval/schema.py) isn't mistaken for a test class.
python_classes = *Suite
testpaths = tests
addopts = -q
